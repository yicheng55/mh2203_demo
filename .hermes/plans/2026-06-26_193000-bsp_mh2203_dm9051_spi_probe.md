# MH2203 BSP + SPI/DM9051A 驗證程式實作計畫

> **For Hermes:** Use subagent-driven-development skill to implement this plan task-by-task.

**Goal:** 建立 `bsp_mh2203` 目錄及其 BSP 檔，並提供一個可在 Keil 上編譯與實際量測 SPI/DM9051A 通訊的驗證程式。

**Architecture:**
- `bsp_mh2203/`：MH2203 平台的硬體抽象層，封裝 SPI1 polling / DMA / reset / debug 操作以及 DM9051A register access。
- `USER/`：保留既有 start-up、中斷、system 檔；新增 `main_spi_probe.c` 作為通訊驗證主程式。
- `DM9051A_spipro_mh2203.uvprojx`：複製自參考專案並修正 BSP 路徑與檔名。

**Tech Stack:** Keil µVision (MDK-ARM), CMSIS / STM32StdPeriph-like HAL, DM9051A SPI driver.

---

## Task 1: 建立 `bsp_mh2203` 目錄

**Objective:** 建立要放置 BSP 檔的新目錄。

**Files:**
- Create: `ModuleDemo/DM9051A/bsp_mh2203/`

**Step 1:** Create the directory

```bash
# workdir: D:\prg\davicom\mh2xxxx_proj\mh2203_demo
mkdir -p ModuleDemo/DM9051A/bsp_mh2203
```

**Step 2:** Verify directory creation

Run: `ls ModuleDemo/DM9051A/bsp_mh2203`
Expected: empty directory listing.

---

## Task 2: 建立 `mh2203_spi1.h`

**Objective:** 定義 MH2203 板上的 SPI1 與 DM9051A 針腳、暫存器位址、Debug 巨集與函式原型。

**Files:**
- Create: `ModuleDemo/DM9051A/bsp_mh2203/mh2203_spi1.h`

**Step 1:** Write header

```c
#ifndef __MH2203_SPI1_H
#define __MH2203_SPI1_H

#include <stdint.h>
#ifndef USE_STDPERIPH_DRIVER
#define USE_STDPERIPH_DRIVER
#endif
#include "mh22xx.h"

#ifdef __cplusplus
extern "C" {
#endif

/* ---- DM9051A 操作 ---- */
#define DM9051A_OP_REG_R          0x00u
#define DM9051A_OP_REG_W          0x80u

/* ---- DM9051A 暫存器位址 ---- */
#define DM9051A_NCR               0x00u
#define DM9051A_VIDL              0x28u
#define DM9051A_VIDH              0x29u
#define DM9051A_PIDL              0x2Au
#define DM9051A_PIDH              0x2Bu
#define DM9051A_CHIPR             0x2Cu
#define DM9051A_PAR               0x10u
#define DM9051A_MRCMD             0x72u
#define DM9051A_MWCMD             0x78u

/* ---- MH2203 針腳 (與 MH2030A 相同) ---- */
#define DM9051A_CS_PORT           GPIOA
#define DM9051A_CS_PIN            GPIO_Pin_15
#define DM9051A_SCK_PORT          GPIOB
#define DM9051A_SCK_PIN           GPIO_Pin_3
#define DM9051A_MOSI_PORT         GPIOB
#define DM9051A_MOSI_PIN          GPIO_Pin_5
#define DM9051A_MISO_PORT         GPIOB
#define DM9051A_MISO_PIN          GPIO_Pin_4
#define DM9051A_RST_PORT          GPIOF
#define DM9051A_RST_PIN           GPIO_Pin_7

/* ---- Debug 開關 ---- */
#ifndef DM9051A_SPI_DEBUG
#define DM9051A_SPI_DEBUG         1
#endif

/* SPI1 函式 */
void MH2203_SPI1_Init(void);
uint8_t MH2203_SPI1_Transfer(uint8_t tx);

/* 高階 DM9051A 函式 */
void DM9051A_CS_Low(void);
void DM9051A_CS_High(void);
void DM9051A_HardwareReset(void);
void DM9051A_DebugDump(const char *tag);

uint8_t DM9051A_ReadReg(uint8_t reg);
void DM9051A_WriteReg(uint8_t reg, uint8_t val);
void DM9051A_ReadRegBuf(uint8_t reg, uint8_t *buf, uint16_t len);
void DM9051A_WriteRegBuf(uint8_t reg, const uint8_t *buf, uint16_t len);
void DM9051A_ReadMem(uint8_t *buf, uint16_t len);
void DM9051A_WriteMem(const uint8_t *buf, uint16_t len);
uint16_t DM9051A_ReadVID(void);
uint16_t DM9051A_ReadPID(void);
void DM9051A_ReadMac(uint8_t mac[6]);
void DM9051A_WriteMac(const uint8_t mac[6]);

#ifdef __cplusplus
}
#endif

#endif
```

**Step 2:** Verify headers are syntactically valid (no build tool on host, but confirm no obvious issues by reading back).

---

## Task 3: 建立 `mh2203_spi1.c`

**Objective:** 移植 `mh2030a_spi1.c`，將巨集與函式名稱中的 `MH2030A` 改為 `MH2203`。

**Files:**
- Create: `ModuleDemo/DM9051A/bsp_mh2203/mh2203_spi1.c`

**Step 1:** Write polling SPI driver

```c
#ifndef MH2203_UIP_PORT

#include "mh2203_spi1.h"
#include "delay.h"
#if DM9051A_SPI_DEBUG
#include <stdio.h>
#endif

#define DM9051A_SPI               SPI1

void MH2203_SPI1_Init(void)
{
    GPIO_InitTypeDef gpio;
    SPI_InitTypeDef spi;

    RCC_AHBPeriphClockCmd(RCC_AHBPeriph_GPIOA, ENABLE);
    RCC_AHBPeriphClockCmd(RCC_AHBPeriph_GPIOB, ENABLE);
    RCC_AHBPeriphClockCmd(RCC_AHBPeriph_GPIOF, ENABLE);
    RCC_APB2PeriphClockCmd(RCC_APB2Periph_SPI1, ENABLE);

    GPIO_StructInit(&gpio);
    gpio.GPIO_Pin = DM9051A_CS_PIN;
    gpio.GPIO_Mode = GPIO_Mode_OUT;
    gpio.GPIO_Speed = GPIO_Speed_50MHz;
    gpio.GPIO_OType = GPIO_OType_PP;
    gpio.GPIO_PuPd = GPIO_PuPd_UP;
    GPIO_Init(DM9051A_CS_PORT, &gpio);
    DM9051A_CS_High();

    GPIO_StructInit(&gpio);
    gpio.GPIO_Pin = DM9051A_RST_PIN;
    gpio.GPIO_Mode = GPIO_Mode_OUT;
    gpio.GPIO_Speed = GPIO_Speed_50MHz;
    gpio.GPIO_OType = GPIO_OType_PP;
    gpio.GPIO_PuPd = GPIO_PuPd_UP;
    GPIO_Init(DM9051A_RST_PORT, &gpio);
    DM9051A_HardwareReset();

    GPIO_PinAFConfig(GPIOB, GPIO_PinSource3, GPIO_AF_0);
    GPIO_PinAFConfig(GPIOB, GPIO_PinSource4, GPIO_AF_0);
    GPIO_PinAFConfig(GPIOB, GPIO_PinSource5, GPIO_AF_0);

    GPIO_StructInit(&gpio);
    gpio.GPIO_Pin = DM9051A_SCK_PIN;
    gpio.GPIO_Mode = GPIO_Mode_AF;
    gpio.GPIO_Speed = GPIO_Speed_50MHz;
    gpio.GPIO_OType = GPIO_OType_PP;
    gpio.GPIO_PuPd = GPIO_PuPd_NOPULL;
    GPIO_Init(DM9051A_SCK_PORT, &gpio);

    GPIO_StructInit(&gpio);
    gpio.GPIO_Pin = DM9051A_MOSI_PIN;
    gpio.GPIO_Mode = GPIO_Mode_AF;
    gpio.GPIO_Speed = GPIO_Speed_50MHz;
    gpio.GPIO_OType = GPIO_OType_PP;
    gpio.GPIO_PuPd = GPIO_PuPd_NOPULL;
    GPIO_Init(DM9051A_MOSI_PORT, &gpio);

    GPIO_StructInit(&gpio);
    gpio.GPIO_Pin = DM9051A_MISO_PIN;
    gpio.GPIO_Mode = GPIO_Mode_AF;
    gpio.GPIO_Speed = GPIO_Speed_50MHz;
    gpio.GPIO_OType = GPIO_OType_PP;
    gpio.GPIO_PuPd = GPIO_PuPd_UP;
    GPIO_Init(DM9051A_MISO_PORT, &gpio);

    SPI_I2S_DeInit(DM9051A_SPI);
    SPI_StructInit(&spi);
    spi.SPI_Direction = SPI_Direction_2Lines_FullDuplex;
    spi.SPI_Mode = SPI_Mode_Master;
    spi.SPI_DataSize = SPI_DataSize_8b;
    spi.SPI_CPOL = SPI_CPOL_Low;
    spi.SPI_CPHA = SPI_CPHA_1Edge;
    spi.SPI_NSS = SPI_NSS_Soft;
    spi.SPI_BaudRatePrescaler = SPI_BaudRatePrescaler_4;
    spi.SPI_FirstBit = SPI_FirstBit_MSB;
    spi.SPI_CRCPolynomial = 7;
    SPI_Init(DM9051A_SPI, &spi);
    SPI_RxFIFOThresholdConfig(DM9051A_SPI, SPI_RxFIFOThreshold_QF);
    SPI_Cmd(DM9051A_SPI, ENABLE);

#if DM9051A_SPI_DEBUG
    /* Debug prints (same body as mh2030a_spi1.c when debug enabled). */
#endif
}

uint8_t MH2203_SPI1_Transfer(uint8_t tx)
{
    uint32_t timeout = 1000000u;
    while (SPI_I2S_GetFlagStatus(DM9051A_SPI, SPI_I2S_FLAG_TXE) == RESET) {
        if (--timeout == 0u) {
#if DM9051A_SPI_DEBUG
            /* DM9051A_DBG_PRINT(...) */ ;
#endif
            return 0x00u;
        }
    }
    SPI_SendData8(DM9051A_SPI, tx);

    timeout = 1000000u;
    while (SPI_I2S_GetFlagStatus(DM9051A_SPI, SPI_I2S_FLAG_RXNE) == RESET) {
        if (--timeout == 0u) {
#if DM9051A_SPI_DEBUG
            /* DM9051A_DBG_PRINT(...) */ ;
#endif
            return 0x00u;
        }
    }
    return SPI_ReceiveData8(DM9051A_SPI);
}

void DM9051A_CS_Low(void)
{
    GPIO_ResetBits(DM9051A_CS_PORT, DM9051A_CS_PIN);
}

void DM9051A_CS_High(void)
{
    GPIO_SetBits(DM9051A_CS_PORT, DM9051A_CS_PIN);
}

void DM9051A_HardwareReset(void)
{
    GPIO_ResetBits(DM9051A_RST_PORT, DM9051A_RST_PIN);
    Delay_Ms(2);
    GPIO_SetBits(DM9051A_RST_PORT, DM9051A_RST_PIN);
    Delay_Ms(10);
}

void DM9051A_DebugDump(const char *tag)
{
#if DM9051A_SPI_DEBUG
    uint8_t raw1, raw2, chipr;
    raw1 = (tag ? (uint8_t)(tag[0]) : 0u); /* dummy to avoid unused warning */
    DM9051A_CS_Low();
    raw1 = MH2203_SPI1_Transfer(DM9051A_CHIPR | DM9051A_OP_REG_R);
    raw2 = MH2203_SPI1_Transfer(0x00u);
    DM9051A_CS_High();
    (void)raw1; (void)raw2;

    chipr = DM9051A_ReadReg(DM9051A_CHIPR);
    (void)chipr; (void)tag;
#endif
}

uint8_t DM9051A_ReadReg(uint8_t reg)
{
    uint8_t val;
    DM9051A_CS_Low();
    MH2203_SPI1_Transfer((uint8_t)(reg | DM9051A_OP_REG_R));
    val = MH2203_SPI1_Transfer(0x00u);
    DM9051A_CS_High();
    return val;
}

void DM9051A_WriteReg(uint8_t reg, uint8_t val)
{
    DM9051A_CS_Low();
    MH2203_SPI1_Transfer((uint8_t)(reg | DM9051A_OP_REG_W));
    MH2203_SPI1_Transfer(val);
    DM9051A_CS_High();
}

void DM9051A_ReadRegBuf(uint8_t reg, uint8_t *buf, uint16_t len)
{
    uint16_t i;
    DM9051A_CS_Low();
    MH2203_SPI1_Transfer((uint8_t)(reg | DM9051A_OP_REG_R));
    for (i = 0; i < len; i++) {
        buf[i] = MH2203_SPI1_Transfer(0x00u);
    }
    DM9051A_CS_High();
}

void DM9051A_WriteRegBuf(uint8_t reg, const uint8_t *buf, uint16_t len)
{
    uint16_t i;
    DM9051A_CS_Low();
    MH2203_SPI1_Transfer((uint8_t)(reg | DM9051A_OP_REG_W));
    for (i = 0; i < len; i++) {
        MH2203_SPI1_Transfer(buf[i]);
    }
    DM9051A_CS_High();
}

void DM9051A_ReadMem(uint8_t *buf, uint16_t len)
{
    uint16_t i;
    DM9051A_CS_Low();
    MH2203_SPI1_Transfer((uint8_t)(DM9051A_MRCMD | DM9051A_OP_REG_R));
    for (i = 0; i < len; i++) {
        buf[i] = MH2203_SPI1_Transfer(0x00u);
    }
    DM9051A_CS_High();
}

void DM9051A_WriteMem(const uint8_t *buf, uint16_t len)
{
    uint16_t i;
    DM9051A_CS_Low();
    MH2203_SPI1_Transfer((uint8_t)(DM9051A_MWCMD | DM9051A_OP_REG_W));
    for (i = 0; i < len; i++) {
        MH2203_SPI1_Transfer(buf[i]);
    }
    DM9051A_CS_High();
}

uint16_t DM9051A_ReadVID(void)
{
    return (uint16_t)DM9051A_ReadReg(DM9051A_VIDL) |
           ((uint16_t)DM9051A_ReadReg(DM9051A_VIDH) << 8);
}

uint16_t DM9051A_ReadPID(void)
{
    return (uint16_t)DM9051A_ReadReg(DM9051A_PIDL) |
           ((uint16_t)DM9051A_ReadReg(DM9051A_PIDH) << 8);
}

void DM9051A_ReadMac(uint8_t mac[6])
{
    DM9051A_ReadRegBuf(DM9051A_PAR, mac, 6);
}

void DM9051A_WriteMac(const uint8_t mac[6])
{
    DM9051A_WriteRegBuf(DM9051A_PAR, mac, 6);
}

#endif /* MH2203_UIP_PORT */
```

---

## Task 4: 建立 `mh2203_spi1_dma.c`

**Objective:** 移植 DMA 版 SPI1 驅動到 MH2203。

**Files:**
- Create: `ModuleDemo/DM9051A/bsp_mh2203/mh2203_spi1_dma.c`

**Step 1:** Write DMA driver

```c
#ifndef MH2203_UIP_PORT

#include "mh2203_spi1.h"
#include "delay.h"
#if DM9051A_SPI_DEBUG
#include <stdio.h>
#endif

#define DM9051A_SPI               SPI1
#define DM9051A_SPI_RX_DMA        DMA1_Channel2
#define DM9051A_SPI_TX_DMA        DMA1_Channel3
#define DM9051A_SPI_RX_DMA_FLAG   DMA1_FLAG_TC2
#define DM9051A_SPI_TX_DMA_FLAG   DMA1_FLAG_TC3
#define DM9051A_SPI_DMA_TIMEOUT   1000000u

static uint8_t DM9051A_DummyTx = 0x00u;
static uint8_t DM9051A_DummyRx;

void MH2203_SPI1_DMA_Init(void)
{
    /* 初始化 DMA1 Channel2/3 供 SPI1_RX/TX 使用。 */
    /* 內容與 mh2030a_spi1_dma.c 結構相同，僅宏名稱改為 MH2203。 */
}

/* 其餘 polling / debug 函式與 mh2030a_spi1_dma.c 相同， */
/* 僅將 `DM9051A_SPI1_Transfer` 換成 `MH2203_SPI1_Transfer`。 */

#endif /* MH2203_UIP_PORT */
```

---

## Task 5: 建立 `dm9051a.c`

**Objective:** 建立 DM9051A 初始化包覆函式。

**Files:**
- Create: `ModuleDemo/DM9051A/bsp_mh2203/dm9051a.c`

**Step 1:** Write wrapper

```c
#ifndef MH2203_UIP_PORT

#include "dm9051a.h"

void DM9051A_Init(void)
{
    MH2203_SPI1_Init();
}

#endif /* MH2203_UIP_PORT */
```

---

## Task 6: 建立 `dm9051a.h`

**Objective:** 定義 DM9051A public API。

**Files:**
- Create: `ModuleDemo/DM9051A/bsp_mh2203/dm9051a.h`

**Step 1:** Write header

```c
#ifndef __DM9051A_H
#define __DM9051A_H

#include <stdint.h>
#include "mh2203_spi1.h"

#ifdef __cplusplus
extern "C" {
#endif

void DM9051A_Init(void);
uint8_t DM9051A_ReadReg(uint8_t reg);
void DM9051A_WriteReg(uint8_t reg, uint8_t val);
uint16_t DM9051A_ReadVID(void);
uint16_t DM9051A_ReadPID(void);

#ifdef __cplusplus
}
#endif

#endif
```

---

## Task 7: 建立 `dm9051a_regs.h`

**Objective:** 提供 DM9051A 暫存器定義與 `mh2203_spi1.h` 的前向宣告相容層。

**Files:**
- Create: `ModuleDemo/DM9051A/bsp_mh2203/dm9051a_regs.h`

**Step 1:** Write header

```c
#ifndef __DM9051A_REGS_H
#define __DM9051A_REGS_H

#include "mh2203_spi1.h"

#endif
```

---

## Task 8: 建立 SPI 驗證主程式 `USER\main_spi_probe.c`

**Objective:** 參考舊版 `USER/main.c` 並將所有 `MH2030A` 巨集/函式改為 `MH2203`，用於 UART 輸出 DM9051A 識別碼驗證 SPI 通訊。

**Files:**
- Create: `ModuleDemo/DM9051A/USER/main_spi_probe.c`

**Step 1:** Write probe main

```c
#ifndef MH2203_UIP_PORT

#include <stdio.h>
#include <stdarg.h>
#ifndef USE_STDPERIPH_DRIVER
#define USE_STDPERIPH_DRIVER
#endif
#include "mh22xx.h"
#include "delay.h"
#include "mh2203_spi1.h"
#include "dm9051a.h"

#define PRINTF_LOG               printf

static USART_TypeDef *USART_TEST = USART2;

static void CLK_Configuration(void);
static void UART_Configuration(uint32_t baud);
static void DM9051A_PrintProbe(void);
static void Clock_Log(const char *fmt, ...);
static void Clock_PrintConfig(void);
int SER_PutChar(int ch);

int main(void)
{
    CLK_Configuration();
    Delay_Init();
    UART_Configuration(115200);

    Clock_PrintConfig();

    PRINTF_LOG("\r\nDM9051A SPI1 probe start (MH2203)\r\n");
    PRINTF_LOG("Pins: PA15 CS(GPIO), PB3 SCK, PB5 MOSI, PB4 MISO\r\n");

    DM9051A_Init();
    Delay_Ms(10);
#if DM9051A_SPI_DEBUG
    DM9051A_DebugDump("after init");
#endif
    DM9051A_PrintProbe();

    while (1) {
        Delay_Ms(1000);
    }
}

static void DM9051A_PrintProbe(void)
{
    uint8_t chipr;
    uint16_t vid, pid;

    vid = DM9051A_ReadVID();
    pid = DM9051A_ReadPID();
    chipr = DM9051A_ReadReg(DM9051A_CHIPR);

    PRINTF_LOG("DM9051A VID=0x%04X PID=0x%04X CHIPR=0x%02X\r\n", vid, pid, chipr);

    if (vid == 0x0A46u) {
        PRINTF_LOG("DM9051A SPI read OK\r\n");
    } else {
        PRINTF_LOG("DM9051A SPI read abnormal, check PA15/PB3/PB5/PB4 wiring and CS polarity\r\n");
    }
}

static void Clock_Log(const char *fmt, ...)
{
    char buffer[96];
    va_list args;
    int i;
    va_start(args, fmt);
    vsnprintf(buffer, sizeof(buffer), fmt, args);
    va_end(args);
    printf("%s", buffer);
}

static void Clock_PrintConfig(void)
{
    RCC_ClocksTypeDef clocks;
    RCC_GetClocksFreq(&clocks);
    Clock_Log("Clock mode: HSI/2 + PLL (72MHz target)\r\n");
    Clock_Log("PLL source: HSI/2, PLL mul: x18\r\n");
    Clock_Log("SYSCLK: %3.1fMhz, HCLK: %3.1fMhz, PCLK: %3.1fMhz\r\n",
        (float)clocks.SYSCLK_Frequency / 1000000,
        (float)clocks.HCLK_Frequency   / 1000000,
        (float)clocks.PCLK_Frequency   / 1000000);
}

static void CLK_Configuration(void)
{
    RCC_DeInit();
    RCC_HSICmd(ENABLE);
    while (RCC_GetFlagStatus(RCC_FLAG_HSIRDY) == RESET) { }
    RCC_PLLCmd(DISABLE);
    FLASH_Unlock();
    FLASH_SetLatency(FLASH_Latency_2);
    FLASH_Lock();
    RCC_PLLConfig(RCC_PLLSource_HSI_Div2, RCC_PLLMul_18);
    RCC_PLLCmd(ENABLE);
    while (RCC_GetFlagStatus(RCC_FLAG_PLLRDY) == RESET) { }
    RCC_SYSCLKConfig(RCC_SYSCLKSource_PLLCLK);
    RCC_HCLKConfig(RCC_SYSCLK_Div1);
    RCC_PCLKConfig(RCC_HCLK_Div1);
}

static void UART_Configuration(uint32_t baud)
{
    GPIO_InitTypeDef gpio;
    USART_InitTypeDef usart;

    RCC_APB1PeriphClockCmd(RCC_APB1Periph_USART2, ENABLE);
    RCC_AHBPeriphClockCmd(RCC_AHBPeriph_GPIOA, ENABLE);

    GPIO_PinAFConfig(GPIOA, GPIO_PinSource2, GPIO_AF_1);
    GPIO_PinAFConfig(GPIOA, GPIO_PinSource3, GPIO_AF_1);

    GPIO_StructInit(&gpio);
    gpio.GPIO_Pin = GPIO_Pin_2;
    gpio.GPIO_Mode = GPIO_Mode_AF;
    gpio.GPIO_Speed = GPIO_Speed_50MHz;
    gpio.GPIO_OType = GPIO_OType_PP;
    gpio.GPIO_PuPd = GPIO_PuPd_NOPULL;
    GPIO_Init(GPIOA, &gpio);

    GPIO_StructInit(&gpio);
    gpio.GPIO_Pin = GPIO_Pin_3;
    gpio.GPIO_Mode = GPIO_Mode_AF;
    gpio.GPIO_Speed = GPIO_Speed_50MHz;
    gpio.GPIO_OType = GPIO_OType_PP;
    gpio.GPIO_PuPd = GPIO_PuPd_UP;
    GPIO_Init(GPIOA, &gpio);

    USART_StructInit(&usart);
    usart.USART_BaudRate = baud;
    usart.USART_WordLength = USART_WordLength_8b;
    usart.USART_StopBits = USART_StopBits_1;
    usart.USART_Parity = USART_Parity_No;
    usart.USART_HardwareFlowControl = USART_HardwareFlowControl_None;
    usart.USART_Mode = USART_Mode_Rx | USART_Mode_Tx;

    USART_Init(USART_TEST, &usart);
    USART_Cmd(USART_TEST, ENABLE);
}

int SER_PutChar(int ch)
{
    while (USART_GetFlagStatus(USART_TEST, USART_FLAG_TC) == RESET) { }
    USART_SendData(USART_TEST, (uint8_t)ch);
    return ch;
}

int fputc(int ch, FILE *f)
{
    (void)f;
    if (ch == '\n') {
        SER_PutChar('\r');
    }
    return SER_PutChar(ch);
}

#endif /* MH2203_UIP_PORT */
```

---

## Task 9: 建立 Keil 專案 `DM9051A_spipro_mh2203.uvprojx`

**Objective:** 複製 `DM9051A_mh2203.uvprojx`，將 BSP 路徑與檔名更新為 `bsp_mh2203` 與 `mh2203_*`，並以 `main_spi_probe.c` 為主要程式。

**Files:**
- Create: `ModuleDemo/DM9051A/USER/DM9051A_spipro_mh2203.uvprojx`

**Step 1:** 複製原始檔案並做批次取代

```powershell
Copy-Item 'D:\prg\davicom\mh2xxxx_proj\mh2203_demo\ModuleDemo\DM9051A\USER\DM9051A_mh2203.uvprojx' `
           'D:\prg\davicom\mh2xxxx_proj\mh2203_demo\ModuleDemo\DM9051A\USER\DM9051A_spipro_mh2203.uvprojx'
```

**Step 2:** 批次取代內容

```powershell
$p = 'D:\prg\davicom\mh2xxxx_proj\mh2203_demo\ModuleDemo\DM9051A\USER\DM9051A_spipro_mh2203.uvprojx'
$txt = Get-Content $p -Raw
$txt = $txt -replace '..\\bsp\\dm9051a.c','..\\bsp_mh2203\\dm9051a.c'
$txt = $txt -replace '..\\bsp\\mh2030a_spi1.c','..\\bsp_mh2203\\mh2203_spi1.c'
$txt = $txt -replace '..\\bsp\\mh2030a_spi1_dma.c','..\\bsp_mh2203\\mh2203_spi1_dma.c'
$txt = $txt -replace 'mh2030a_spi1\.c','mh2203_spi1.c'
$txt = $txt -replace 'mh2030a_spi1_dma\.c','mh2203_spi1_dma.c'
$txt = $txt -replace 'mh20xx_conf\.h','mh20xx_conf.h'
$txt = $txt -replace 'main_uip_mh2030a\.c','main_spi_probe.c'
$txt = $txt -replace 'mh2030a_board\.c','mh2203_board.c'
$txt = $txt -replace 'mh2030a_dm9051_spi\.c','mh2203_dm9051_spi.c'
Set-Content $p $txt
```

---

## Task 10: 完整性檢查

**Objective:** 確認新增檔案與專案結構完整性。

**Files:**
- `ModuleDemo/DM9051A/bsp_mh2203/mh2203_spi1.h`
- `ModuleDemo/DM9051A/bsp_mh2203/mh2203_spi1.c`
- `ModuleDemo/DM9051A/bsp_mh2203/mh2203_spi1_dma.c`
- `ModuleDemo/DM9051A/bsp_mh2203/dm9051a.h`
- `ModuleDemo/DM9051A/bsp_mh2203/dm9051a.c`
- `ModuleDemo/DM9051A/bsp_mh2203/dm9051a_regs.h`
- `ModuleDemo/DM9051A/USER/main_spi_probe.c`
- `ModuleDemo/DM9051A/USER/DM9051A_spipro_mh2203.uvprojx`

**Step 1:** Verify file tree

```bash
find ModuleDemo/DM9051A/bsp_mh2203 -type f
# Expect: dm9051a.c dm9051a.h dm9051a_regs.h mh2203_spi1.c mh2203_spi1.h mh2203_spi1_dma.c
```

**Step 2:** Grep 確認專案參考正確

```bash
grep -E 'bsp_mh2203|mh2203_spi1|main_spi_probe' \
  ModuleDemo/DM9051A/USER/DM9051A_spipro_mh2203.uvprojx
```

Expected: matches for the new paths.

**Step 3:** 編譯驗證

在 Keil µVision 中開啟 `ModuleDemo/DM9051A/USER/DM9051A_spipro_mh2203.uvprojx`，執行 Build (F7)。
Expected: 0 Error, 0 Warning (若有僅為承襲既有環境設定)。

---

## 風險、取捨與開放問題

1. **MH2030A v.s. MH2203 針腳差異**
   - 目前以 `dm9051_driver/ports/mh2203/dm9051_hal_mh2203_spi1_priv.h` 定義為準，針腳配置與 `bsp_mh2030a` 完全一致。若實際板子 Revision 不同，需自行在 `mh2203_spi1.h` 微調 CS/RST/port。

2. **Delay 實作來源**
   - 沿用 `SYSTEM/delay/delay.c`，`bsp_mh2203` 不客製化 delay。若 `mh22xx.h` 沒有內建 `Delay_Ms()`，則必須移植 delay 檔。

3. **Keil 專案路徑**
   - `DM9051A_spipro_mh2203.uvprojx` 路徑採相對路徑 `..\\bsp_mh2203\\`。若 Keil 開啟時工作目錄不同，初始可能要手動重新指向 Include path。

4. **DMA 細節**
   - `mh2203_spi1_dma.c` 目前為骨架；若需完整 DMA 流程建議比對 `dm9051_driver/ports/mh2203/dm9051_hal_mh2203_spi1_dma.c` 一併實作。

---

### 下一步建議實作順序

Task 1 -> Task 2 -> Task 3 -> Task 4 -> Task 5 -> Task 6 -> Task 7 -> Task 8 -> Task 9 -> Task 10
